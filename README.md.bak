# Ekşi Sözlük Clone - Next.js (TypeScript)

## 1. Proje Tanımı
Bu proje, **Ekşi Sözlük benzeri** bir sözlük platformudur. Kullanıcılar yeni **entry**'ler ekleyebilir, başlıklar açabilir ve yorum yapabilirler. Proje **Next.js, TypeScript, MongoDB ve Tailwind CSS** kullanılarak geliştirilmiştir.

---

## 2. Teknoloji Yığını
| Teknoloji | Kullanım Amacı |
|-----------|----------------|
| **Next.js (TypeScript)** | SSR/SSG desteği ve React tabanlı frontend |
| **MongoDB** | Veritabanı (MongoDB Atlas) |
| **Mongoose** | MongoDB ORM |
| **NextAuth.js** | Kullanıcı kimlik doğrulama |
| **Tailwind CSS** | Responsive tasarım |
| **Zustand** | Global state yönetimi |
| **Socket.io** | Gerçek zamanlı veri güncellemesi |
| **React-Markdown** | Entry'lerde Markdown desteği |
| **Axios** | API istekleri |
| **React Query** | Veri yönetimi ve cache optimizasyonu |

---

## 3. Proje Kurulumu

### **3.1. Projeyi Klonla**
```bash
 git clone https://github.com/kullanici/eksi-sozluk-clone.git
 cd eksi-sozluk-clone
```

### **3.2. TypeScript ve Bağımlılıkları Kur**
```bash
npm install
npm install --save-dev typescript @types/react @types/node @types/express @types/mongoose @types/socket.io-client @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint eslint-config-next
```

### **3.3. TypeScript Konfigürasyon Dosyası (`tsconfig.json`)**
```json
{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "strict": true,
    "jsx": "preserve",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "strictPropertyInitialization": true
  }
}
```

### **3.4. TypeScript Hata Kontrolleri ve ESLint Konfigürasyonu**
```bash
npx eslint --init
```
`.eslintrc.json` dosyası içerisine şu konfigürasyonu ekleyelim:
```json
{
  "extends": ["next/core-web-vitals", "plugin:@typescript-eslint/recommended"],
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/explicit-module-boundary-types": "warn"
  }
}
```

### **3.5. .env.local Dosyasını Oluştur**
```env
MONGO_URI=mongodb+srv://kullanici:parola@cluster.mongodb.net/myDatabase
NEXTAUTH_SECRET=someRandomSecret
```

### **3.6. Geliştirme Ortamını Başlat**
```bash
npm run dev
```

---

## 4. Performans Optimizasyonu
- **Lazy Loading** ile bileşen ve görseller yüklemesi optimize edildi.
- **React Query Cache** ile gereksiz API çağrıları engellendi.
- **Next.js ISR (Incremental Static Regeneration)** kullanarak sayfa yenilemeleri hızlandırıldı.
- **UseMemo ve UseCallback** hook'ları ile gereksiz render işlemleri önlendi.
- **Tailwind CSS Purge** ile kullanılmayan stiller kaldırıldı.
- **Next.js Image Component** ile görsel optimizasyon sağlandı.

---

## 5. Yeni Özellikler

### **5.1. Entry Beğeni (Like) Sistemi**
- Kullanıcılar entry'leri beğenebilir ve beğenilerini geri alabilir.
- `src/pages/api/likes.ts` dosyası oluşturuldu.
- `LikeButton.tsx` bileşeni oluşturuldu ve entry'lerde kullanılacak.

### **5.2. Entry Yorumları**
- Kullanıcılar entry'lere yorum ekleyebilir, silebilir.
- `Comment.ts` modeli oluşturuldu.
- `CommentSection.tsx` bileşeni oluşturuldu ve entry detay sayfasında yer alacak.

### **5.3. Entry Sıralama (En Popüler & En Yeni)**
- `rankings.ts` API route'u ile entry'leri beğeni sayısına ve tarihe göre sıralayabiliriz.
- Anasayfa'da sıralama seçeneği eklendi.

### **5.4. Gerçek Zamanlı Güncellemeler (Socket.io)**
- Kullanıcılar yeni entry eklendiğinde **anında görebilecek**.
- **Entry beğenileri ve yorumları** anlık olarak güncellenecek.
- **Gerçek zamanlı bildirimler** ile kullanıcılar yeni entry’lerden haberdar olacak.
- `socket.io` **server ve client bağlantıları** tamamlandı.
- `src/pages/api/socket.ts` oluşturuldu.
- `src/lib/socketClient.ts` ile **frontend tarafında WebSocket** bağlantısı sağlandı.

---

## 6. Sonraki Adımlar
✅ Kullanıcı giriş/kayıt sistemi eklendi (NextAuth.js)
✅ Entry ekleme/silme/düzenleme işlemleri tamamlandı
✅ Beğeni sistemi eklendi (Like) 
✅ Yorum sistemi eklendi (Comment)
✅ Entry sıralama (popüler/yeni) özelliği eklendi
✅ Markdown desteği eklendi (`react-markdown`)
✅ **Gerçek zamanlı güncellemeler (Socket.io) tamamlandı**
✅ SEO optimizasyonu (`next-seo`)
✅ **TypeScript tüm projeye entegre edildi**
✅ **TypeScript hata kontrolleri ve ESLint eklendi**
✅ **Performans optimizasyonları tamamlandı**

**Bu doküman projeyi başlatmak ve anlamak için rehber niteliğindedir. Ekstra isteklerin varsa söyleyebilirsin! 😊**

